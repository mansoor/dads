# ============================================================
# Auto-generated by env-gen.sh — 2026-05-29 21:47:29 UTC
# Environment: stage  |  Project: myapp
# DO NOT COMMIT — contains secrets
# ============================================================

# ── Project ────────────────────────────────────────────────
COMPOSE_PROJECT_NAME=myapp_stage
PROJECT_NAME=myapp
ENV=stage

# ── Image tags ─────────────────────────────────────────────
REGISTRY=registry.example.com
IMAGE_TAG=1.0.0-build.1-stage
BACKEND_IMAGE=registry.example.com/myapp-backend:1.0.0-build.1-stage


# ── Domain & ports ─────────────────────────────────────────
DOMAIN=stage.myapp.com
HTTP_PORT=8180
HTTPS_PORT=8543

# ── Stack config ────────────────────────────────────────────
BACKEND=laravel
FRONTEND=nextjs
FRONTEND_ENABLED=false
DEPLOYMENT=compose
TRAEFIK_ENABLED=true
BACKEND_REPLICAS=1
FRONTEND_REPLICAS=1

# ── Database ───────────────────────────────────────────────
DATABASE=postgres
POSTGRES_HOST=myapp_stage_postgres
POSTGRES_PORT=5432
POSTGRES_DB=myapp_stage
POSTGRES_USER=myapp_user
POSTGRES_PASSWORD=CHANGE_ME_DB_PASSWORD

# ── Application ────────────────────────────────────────────
APP_ENV=stage
APP_DEBUG=false
APP_URL=http://stage.myapp.com

# ── Redis ──────────────────────────────────────────────────
REDIS_ENABLED=true
REDIS_HOST=myapp_stage_redis
REDIS_PORT=6379
REDIS_PASSWORD=

# ── Garage (S3-compatible storage) ─────────────────────────
GARAGE_ENABLED=false


# ── Mail (fill in per-environment) ─────────────────────────
MAIL_DRIVER=smtp
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM_ADDRESS=noreply@stage.myapp.com
MAIL_FROM_NAME="myapp"

# ── Node.js specific ───────────────────────────────────────
NODE_ENV=development
PORT=3000
